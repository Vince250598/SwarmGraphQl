type Event {
	id: ID!
	method: Method!
	session: Session!
	charStart: String
	charEnd: String
	lineNumber: Int
	detail: String
	kind: String
}

extend type Query {
    findAllEvents: [Event]!
}